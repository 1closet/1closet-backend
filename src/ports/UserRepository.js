class UserRepository {
    findByEmail(email) {
        throw new Error('Método não implementado');
    }

    save(user) {
        throw new Error('Método não implementado');
    }
}

module.exports = UserRepository;
