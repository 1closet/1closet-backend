class ProductRepository {
    findById(id) {
        throw new Error('Método não implementado');
    }

    save(product) {
        throw new Error('Método não implementado');
    }
}

module.exports = ProductRepository;
